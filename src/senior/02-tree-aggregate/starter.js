// Senior 2 — recursive aggregation over a tree. See CHALLENGE.md.

export function totalSize(node) {
  // TODO: leaf -> node.size; folder -> sum of totalSize(child).
}

export function countFiles(node) {
  // TODO: leaf -> 1; folder -> sum of countFiles(child).
}
