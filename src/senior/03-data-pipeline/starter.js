// Senior 3 — pure "compute the visible rows" pipeline. See CHALLENGE.md.

export function getView(rows, options = {}) {
  // TODO: filter (query) -> sort (sortKey/sortDir) -> paginate (page/pageSize).
  //       Return { rows, total, totalPages, page }. Do NOT mutate `rows`.
}
