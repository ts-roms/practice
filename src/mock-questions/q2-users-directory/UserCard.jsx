/**
 * Renders one user.
 * Props: user = { id, name, email }
 *
 * TODO: render a card with data-testid="user-{id}" showing name and email.
 */
export default function UserCard({ user }) {
  return (
    <div>
      {/* TODO */}
    </div>
  )
}
