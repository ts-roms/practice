/**
 * Controlled search input.
 * Props: value, onChange(value)
 *
 * TODO: render an <input placeholder="Search users"> bound to value/onChange.
 */
export default function SearchBar({ value, onChange }) {
  return (
    <div>
      {/* TODO: controlled input */}
    </div>
  )
}
