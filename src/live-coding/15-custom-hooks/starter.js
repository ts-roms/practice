import { useState, useRef, useEffect, useCallback } from 'react'

// Live coding 15 — implement these from CHALLENGE.md.

export function useToggle(initial = false) {
  // TODO: return [value, toggle, setValue]; toggle() flips, toggle(bool) sets.
}

export function usePrevious(value) {
  // TODO: return the value from the previous render (undefined on first render).
}
