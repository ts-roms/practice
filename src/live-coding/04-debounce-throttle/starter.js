// Live coding 4 — implement these from CHALLENGE.md.

export function debounce(fn, delay) {
  // TODO: return a function that runs fn only `delay` ms after the last call.
}

export function throttle(fn, limit) {
  // TODO: return a function that runs fn at most once per `limit` ms (leading edge).
}
