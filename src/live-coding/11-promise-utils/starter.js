// Live coding 11 — implement these from CHALLENGE.md (don't use Promise.all).

export function promiseAll(promises) {
  // TODO: resolve to results in order; reject on first rejection.
}

export function retry(fn, attempts) {
  // TODO: call fn(); retry on rejection up to `attempts` total tries.
}
