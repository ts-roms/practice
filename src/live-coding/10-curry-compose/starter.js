// Live coding 10 — implement these from CHALLENGE.md.

export function curry(fn) {
  // TODO: accumulate args until fn.length is reached, then call fn.
}

export function compose(...fns) {
  // TODO: right-to-left. compose(f, g)(x) === f(g(x)).
}

export function pipe(...fns) {
  // TODO: left-to-right. pipe(f, g)(x) === g(f(x)).
}
