import { useState } from 'react'

// Live coding 7 — Tags Input. Build from CHALLENGE.md.
export default function TagsInput() {
  const [tags, setTags] = useState([])
  const [text, setText] = useState('')

  // TODO: addTag (trim, dedupe), removeTag, onKeyDown (Enter / Backspace).

  return (
    <div>
      {/* TODO: chips + input */}
    </div>
  )
}
