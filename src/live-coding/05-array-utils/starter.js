// Live coding 5 — implement these from CHALLENGE.md (no Array.prototype.flat).

export function flatten(arr) {
  // TODO: deeply flatten to any depth.
}

export function groupBy(arr, keyFn) {
  // TODO: bucket items into { key: [items] } using keyFn(item).
}
